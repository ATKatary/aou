import subprocess
from utils import *

def gen_img_2_mesh(img, name="result"):
    pass
    # subprocess.run(f"python run.py configs/instant-mesh-large.yaml {img_path} --output_path {BASE_DIR}/out".split(' '))
